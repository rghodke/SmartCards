<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.admin.smartcards.EditDeck"
    android:background="#ffb5bfc4">

    <LinearLayout android:layout_width="fill_parent"
        android:layout_height="75dp"
        android:orientation="horizontal"
        android:id="@+id/linearLayout"
        android:background="#ff888f93">

        <ImageButton android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="#ff888f93"
            android:id="@+id/BackButton"
            android:src="@drawable/back"
            android:onClick="goBack"
            android:scaleType="fitCenter"
            android:paddingTop="22dp"
            android:paddingBottom="22dp"
            android:paddingRight="100dp" />

    </LinearLayout>

    <EditText

        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/editDeckName"
        android:layout_below="@+id/linearLayout"
        android:ellipsize="start"
        android:gravity="center_vertical|center_horizontal"
        android:hint="Enter Deck Name"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:layout_margin="20dp"
        android:background="#ffffffff"
        android:imeOptions="actionDone"
        android:inputType="textAutoCorrect" />

    <EditText

        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/editDeckCourse"
        android:layout_below="@+id/editDeckName"
        android:ellipsize="start"
        android:gravity="center_vertical|center_horizontal"
        android:hint="Enter Deck Course"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:layout_margin="20dp"
        android:background="#ffffffff"
        android:imeOptions="actionDone"
        android:inputType="textAutoCorrect" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:id="@+id/editDeckButton"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/editDeckCourse"
        android:text="Edit Value"
        android:textSize="20sp"
        android:background="@android:color/transparent"
        android:textColor="#ffffffff"
        android:layout_weight="1"
        android:onClick="editConfirm"/>

</RelativeLayout>
